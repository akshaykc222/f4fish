import 'package:flutter/material.dart';

class NearestLocationScreen extends StatelessWidget {
  const NearestLocationScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
